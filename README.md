# AIMA, גרסת GitHub Pages

זוהי חבילה סטטית מלאה של האתר. הגולשים לא צריכים חשבון ChatGPT ולא צריכים להתחבר בכלל.

## העלאה ראשונה ל-GitHub

1. חלצו את קובץ ה-ZIP ופתחו את התיקייה `AIMA-GitHub`.
2. פתחו Repository חדש ב-GitHub בשם `aima-site` ובחרו Public.
3. העלו לשורש ה-Repository את **כל הקבצים והתיקיות שנמצאים בתוך** `AIMA-GitHub`, כולל `index.html`, `assets`, `content`, `studio` והתיקייה הנסתרת `.github`.
4. בסיום, `index.html` חייב להופיע ישירות במסך הראשי של ה-Repository. אל תעלו רק את `index.html`, ואל תיצרו מצב שבו הוא נמצא בתוך `AIMA-GitHub/AIMA-GitHub`.
5. ב-GitHub פתחו `Settings` ואז `Pages`.
6. תחת `Build and deployment`, בחרו `GitHub Actions`.
7. אחרי כדקה פתחו את לשונית `Actions` וודאו שהפעולה `Publish AIMA site` הסתיימה בירוק.

אפשר לפתוח את `index.html` מהמחשב לבדיקה מהירה של העיצוב, אבל התפקוד המלא של הקישורים וה-Studio נבדק דרך אתר GitHub Pages לאחר ההעלאה.

## חיבור הדומיין

1. ב-`Settings` ואז `Pages`, כתבו את הדומיין בשדה `Custom domain` ושמרו.
2. אצל ספק הדומיין הוסיפו את רשומות ה-DNS ש-GitHub מציג לכם. עבור `www` זו בדרך כלל רשומת CNAME אל `<github-user>.github.io`.
3. אחרי שהבדיקה עוברת, סמנו `Enforce HTTPS`.

אל תנחשו רשומות DNS. העתיקו את הערכים המדויקים ש-GitHub מציג במסך של ה-Repository שלכם.

## Studio ניהולי מוגן

פתחו באתר שלכם את `/studio/`. Cloudflare Access מגן על העמוד ומאפשר כניסה רק לכתובת האימייל שהוגדרה במדיניות `Tomer only`.

1. החליפו את אמן החודש, הוסיפו שירים לרוטציה השבועית, או הוסיפו אירוע, עדכון ומשאב.
2. לחצו `הורדת קובץ תוכן`.
3. ב-GitHub החליפו את הקובץ `content/community-overrides.json` בקובץ שירד.
4. בצעו Commit. האתר יתעדכן אוטומטית.

שיר השבוע מתחלף אוטומטית פעם בשבוע מתוך רשימת השירים שב-Studio. אמן החודש נשאר קבוע עד שתבחרו אמן אחר.

כלי התוכן והכתבה ממשיכים להוריד קבצים מוכנים ל־GitHub. מערכת תמונות האמנים פועלת אחרת ומעדכנת את האתר ישירות לאחר אישור.

## בקשות ועדכון תמונות אמנים

- הקישור `עדכון תמונת אמן` מוביל אל `/photo-update/`.
- האמן בוחר את הפרופיל שלו, מעלה תמונה ומאשר שיש לו רשות לפרסם אותה.
- הבקשה מופיעה אוטומטית בתוך ה־Studio, כולל תמונה, שם האמן, פרטי הפונה והערה.
- לחיצה על `אישור ועדכון באתר` חותכת ומכווצת את התמונה, שומרת אותה ומעדכנת את כרטיס האמן.
- לחיצה על `דחיית הבקשה` מוחקת את התמונה שהוגשה.
- אין צורך להוריד תמונה, לערוך JSON או להעלות קובץ ל־GitHub.

המערכת משתמשת ב־Cloudflare Worker, D1 ו־R2. הוראות ההקמה נמצאות בקובץ `cloudflare/photo-api/README-HE.md`. בנוסף להגנה הקיימת על `/studio/*`, חובה להגן ב־Cloudflare Access גם על `/api/admin/*` באותה מדיניות מנהל. אין להשאיר את שדה הנתיב ריק, כדי שהאתר הציבורי יישאר פתוח.

## עדכון אוטומטי של אינדקס האמנים

האתר מחובר לגיליון התשובות של טופס האמנים. פעולת GitHub בודקת את הגיליון פעם ביום, מנקה קישורים, מאחדת הגשות כפולות ומפרסמת את אינדקס האמנים המעודכן.

כדי שהסנכרון יעבוד, הגיליון צריך להיות זמין לצפייה באמצעות הקישור, ללא כניסה לחשבון Google. אין צורך לתת הרשאת עריכה. אם הגיליון אינו נגיש או מוחזר בפורמט לא תקין, הסנכרון נעצר והאתר ממשיך להציג את הרשימה האחרונה שנבדקה.

אפשר להריץ עדכון מיידי דרך `Actions`, לבחור `Publish AIMA site` ואז `Run workflow`.

## הפעלת סטטיסטיקות ו-Meta Pixel

האתר מגיע עם באנר הסכמה ונשאר ללא מעקב עד שתגדירו מזהים והמבקר יאשר:

1. צרו נכס Google Analytics 4 והעתיקו את ה-Measurement ID שמתחיל ב-`G-`.
2. אם צריך, צרו Meta Pixel והעתיקו את המספר שלו.
3. פתחו באתר את `/studio/`, עברו לאזור "סטטיסטיקות ו-Meta Pixel" והדביקו את המזהים.
4. הורידו את `tracking-config.js` והחליפו ב-GitHub את הקובץ בעל אותו השם בשורש האתר.
5. בצעו Commit. המדידה תופעל רק למבקרים שבחרו את הקטגוריה המתאימה.

Google Analytics מציג צפיות, משתמשים, עמודים פופולריים, מקורות הגעה, מדינות ומכשירים. הוא אינו מציג רשימה שמית של האנשים שנכנסו. Meta Pixel מיועד בעיקר למדידת קמפיינים ויצירת קהלים ב-Meta.

## פרטיות ונגישות

- `privacy/index.html` מכיל את מדיניות הפרטיות והעוגיות.
- `accessibility/index.html` מכיל את הצהרת הנגישות.
- נגן YouTube אינו נטען לפני לחיצה מפורשת.
- כלי Google ו-Meta אינם נטענים לפני הסכמה.
- האתר כולל דילוג לתוכן, מיקוד מקלדת, תמיכה בהפחתת תנועה ותפריט התאמות נגישות.

## הגנת תוכן

החבילה מקשה על העתקה רגילה, שמירה, גרירה ותפריט לחיצה ימנית, בלי לחסום מנועי חיפוש, תצוגות מקדימות ברשתות או התאמה לנייד. חשוב לדעת שאין דרך למנוע לחלוטין העתקה של אתר ציבורי, כי הדפדפן חייב לקבל את הקוד כדי להציג אותו.

## קישורים מרכזיים שכבר מחוברים

- קבוצת Facebook ו-WhatsApp
- אינדקס 91 אמנים ייחודיים, לאחר איחוד כפילויות ועדכון הקישורים, עם סנכרון יומי מהטופס
- טופס תמונת אמן ותור אישור מאובטח ב־Studio
- 41 משאבים, מדריכים וכלים
- רדיו ZIK, הרשמה והגשת שירים לרוטציה
- ארכיון AIMA ו-SUNO IL עם 106FM וגיא רפאל
- אירועי הקהילה והרשמה לתוכניות הקרובות
- עמוד הכנה לפודקאסט הקהילתי שבדרך
- קישור התמיכה ב-PayBox
- Prompt Lab מורחב עם בונה פרומפטים, Exclude, טריקים, תגיות מבנה ומתכונים
- אמן החודש ושיר השבוע ברוטציה אוטומטית
- אזור חדשות ועדכונים עם סיפורי האמנים וכתבה מלאה על יוצרי Suno בישראל
- מדיניות פרטיות, ניהול הסכמה והצהרת נגישות
